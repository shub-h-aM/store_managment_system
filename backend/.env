# Server Configuration
PORT=5000                # Port your backend server will run on
HOST=localhost           # Host where your backend is running

# MySQL Database Configuration
DB_HOST=localhost        # Host where your MySQL database is running (e.g., localhost or an IP)
DB_USER=root             # Username for MySQL connection
DB_PASSWORD=Shubham@21 # Password for the MySQL user
DB_NAME=store       # The name of your MySQL database
DB_PORT=3306             # Port for MySQL (default is 3306)

# JWT or other secret keys (if you're using them)
JWT_SECRET=mysecretkey   # Secret key for signing JWT tokens

# (Optional) Environment Mode (useful for distinguishing dev/prod)
NODE_ENV=development     # Set this to 'production' when deploying to production
